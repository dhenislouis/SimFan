<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/TextArea_Disable"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center_horizontal"
    android:background="@color/surface_primary_grey">

    <TextView
        android:id="@+id/TextLabelDefault"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Label Name"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        android:layout_marginBottom="4dp" />

    <androidx.cardview.widget.CardView
        android:id="@+id/TextAreaDefault"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:layout_marginBottom="24dp"
        app:cardCornerRadius="38dp"
        app:cardElevation="0dp">

        <EditText
            android:id="@+id/editTextAreaDefault"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="top|start"
            android:background="@android:color/transparent"
            android:text="Input Text"
            android:padding="16dp"
            android:textSize="16sp"
            android:textColor="@color/text_disable"
            android:inputType="textMultiLine"
            android:maxLines="6"
            android:minLines="4"
            android:scrollbars="vertical" />
    </androidx.cardview.widget.CardView>

    <androidx.cardview.widget.CardView
        android:id="@+id/TextAreaOffLabel"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="24dp"
        app:cardCornerRadius="38dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="@android:color/white">

        <EditText
            android:id="@+id/editTextAreaOffLabel"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="top|start"
            android:background="@android:color/transparent"
            android:text="Input Text"
            android:padding="16dp"
            android:textSize="16sp"
            android:textColor="@color/text_disable"
            android:inputType="textMultiLine"
            android:maxLines="6"
            android:minLines="4"
            android:scrollbars="vertical" />
    </androidx.cardview.widget.CardView>

</LinearLayout>
